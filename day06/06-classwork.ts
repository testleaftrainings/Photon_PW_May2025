/**
 * 1. Define the type --> supportedBrowser = Chrome|Edge
2. Define the type --> browserVersion = 131|130
3. Define the type--> browserProfile(by object literal) = supportedBrowser & browserVersion 
4. Define the function and pass the argument (browser infering browserProfile )
5. Launch the browser.
6. Call the function
 */